<h1>Cadastrar Tipo de Imóvel</h1>

<% if @property_type.errors.any? %>
  <ul>
    <% @property_type.errors.messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>  
<% end %>

<%= form_for(@property_type) do |f| %>
    <%= f.label :name, 'Nome' %>
    <%= f.text_field :name %>

    <%= f.submit 'Cadastrar' %>
<% end %>
  